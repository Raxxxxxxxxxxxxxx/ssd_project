# قائمة المهام المتبقية — A-SSD

آخر تحديث: بعد جلسة تصدير/تقليم/تكميم شاملة (export_onnx, export_tflite,
prune, quantize, export_tensorrt) — كل سكربت اختُبر فعلياً وليس مجرد مكتوب.

## 0) الأولوية القادمة الوحيدة الحقيقية: النشر على الجهاز الفعلي

**يوجد وصول فعلي لجهاز Raspberry Pi 4 و/أو Jetson Nano.** كل أدوات التصدير
جاهزة الآن (راجع القسم 1). الخطوات المتبقية لتحقيق الهدف الحقيقي للمشروع:

1. نقل الملفات التالية إلى الجهاز: `exports/tflite_normal_int8/assd_normal_full_integer_quant.tflite`
   (~2MB، الأنسب لـ Raspberry Pi) + الكود (`inference.py`, `utils.py`,
   `stream_server.py`, `adaptive_engine.py`, `run_camera.py`).
2. تثبيت `tflite-runtime` (أخف بكثير من TensorFlow الكامل) على الجهاز، أو
   TensorFlow الكامل إن كانت الذاكرة تسمح.
3. **يحتاج تعديلاً بسيطاً في inference.py**: حالياً يستخدم PyTorch مباشرة
   وليس TFLite - يحتاج دالة استدلال بديلة تستخدم `ai_edge_litert.Interpreter`
   بدل `model.forward()` عند التشغيل على الجهاز (لم يُبنَ بعد - أولوية عالية
   قبل النشر الفعلي).
4. تشغيل استدلال حقيقي وقياس FPS/الحرارة/الطاقة الفعلية، ومقارنتها بجدولي
   4-1 و4-2 بالتقرير (SSD الأصلي: <3 FPS على RPi4 / ~10 FPS على Jetson Nano).
5. **سؤال عملي عند التنفيذ:** هل يوجد وصول شبكي/SSH من بيئة العمل هذه للجهاز
   مباشرة، أم ينفَّذ يدوياً على الجهاز نفسه؟

---

## 1) أدوات التصدير/الضغط — بُنيت واختُبرت فعلياً هذه الجلسة

| الأداة | النتيجة الحقيقية |
|---|---|
| `export_onnx.py` | ✅ يعمل، مطابق لـPyTorch (فرق أقصى 5e-6) |
| `export_tflite.py` (float32/float16) | ✅ يعمل، 7MB/3.5MB، متحقَّق منه فعلياً |
| `export_tflite.py --quantize` (INT8 كامل) | ✅ **يعمل فعلياً - 2MB فقط (~10× أصغر)**، أفضل مسار لـRaspberry Pi |
| `export_tensorrt.py` | كود جاهز، **لم يُختبر** (يحتاج NVIDIA GPU/Jetson) |
| `prune.py` | ✅ يعمل، لكن **30% و10% كلاهما يدمّران الدقة بالكامل** (0%/0.02% mAP) - راجع الملاحظة أدناه |
| `quantize.py` (PyTorch-native INT8) | ✅ يعمل بعد إصلاح عطلين (SE blocks + residual add) لكن **لا يقلّل الحجم فعلياً** (BatchNorm غير مدموج) - استخدم `export_tflite.py --quantize` بدلاً منه |

**ملاحظات هامة يجب معرفتها:**
- **ai_edge_torch (المذكورة بالتقرير) خطيرة**: تخفّض إصدار PyTorch تلقائياً
  وتكسر توافقه مع torchvision بالكامل. استُبدلت بمسار `onnx2tf` (ONNX→TFLite)
  في بيئة Python 3.11 معزولة تماماً (`tflite_env/`) لا علاقة لها بالبيئة
  الرئيسية. **لا تُثبَّت ai_edge_torch في assd_env مطلقاً.**
- **التقليم البنيوي (Pruning) غير قابل للتطبيق بالطريقة الساذجة** على
  MobileNetV3-Small (خلافاً لـVGG-16 الأصلية): حتى 10% تدمّر الدقة بالكامل
  بدون إعادة تدريب. إن أردنا تقليماً فعلياً لاحقاً: نسب أصغر بكثير (1-5%)
  + fine-tuning بعد كل خطوة، وليس قصّاً لمرة واحدة.
- **fbgemm (المذكور بالتقرير) خطأ لـRaspberry Pi**: هو backend لمعالجات x86،
  بينما ARM يحتاج `qnnpack`. صُحِّح في quantize.py.

## 2) الفجوة المتبقية عن 71.4% mAP — ما زال مفتوحاً

النتيجة الحالية: NORMAL=38.78%, TURBO=42.81%, ECONOMY=34.81% (v2).

- [ ] **Ablation Study** حقيقي (تعطيل كل وحدة على حدة وقياس mAP): **لم يُنفَّذ
      بعد** - يحتاج ~4-5 عمليات تدريب متتالية (~3 ساعات لكل واحدة حتى بحقب
      مخفَّضة ~30 epoch) أي 12-16 ساعة إجمالاً. قرار مقصود: لم يُطلَق هذه
      الجلسة لتجنّب التزام غير مراقَب طويل جداً - يحتاج جلسة/قرار مخصَّص.
- [ ] عدد حقب أكبر من 100، أو batch size أكبر (مقيّد حالياً بالـCPU).
- [ ] Random Crop الحالي مبسّط - نسخة SSD الأصلية تستخدم قيود IoU أدق.
- [ ] Knowledge Distillation من نموذج VGG-16 معلَّم (توصية التقرير، قسم 5.4).

## 3) ما تم إنجازه سابقاً (مرجع تاريخي)

- [x] إصلاح خطأي فهرسة الأصناف المزدوجة وعدم تطابق الإحداثيات بالتقييم (v1).
- [x] Data Augmentation حقيقي + K-Means anchors + round-robin + LR warmup (v2).
- [x] تدريب نموذجين كاملين (100 epoch لكل واحد) وتقييمهما على 3 أوضاع.
- [x] تنظيف تاريخ git من دمج خاطئ لـ2GB بيانات، ورفع كل شيء لـ GitHub.
- [x] اختبار `stream_server.py` بالنموذج المدرَّب الحقيقي.

## 4) ملاحظة منهجية (تأكدت للمرة الثالثة هذه الجلسة)

كل ميزة جديدة يجب اختبارها فعلياً فور بنائها، لا الاكتفاء بأنها "يجب أن
تعمل نظرياً". هذه الجلسة وحدها كشفت 4 مفاجآت حقيقية ما كانت لتُكتشف بقراءة
الكود: (1) ai_edge_torch يكسر البيئة بالكامل، (2) SE blocks وresidual add
في MobileNetV3 غير متوافقين مع eager-mode quantization افتراضياً، (3)
pruning الساذج يدمّر الدقة كلياً على هذه المعمارية، (4) fbgemm/qnnpack يهم
فعلياً لـARM. كلها اكتُشفت بالتشغيل الفعلي فقط.
