# قائمة المهام المتبقية — A-SSD

آخر تحديث: 2026-07-29 - **النشر الفعلي على Jetson Nano تحقَّق بالكامل عملياً
للأوضاع الثلاثة**: مسار `benchmark.py --backend onnx` (CPU) يعطي 10.1-21.4
FPS حسب الوضع، ومسار **TensorRT+GPU يعطي 95.8-176.7 FPS** (تسريع 8.3×-9.5×) -
راجع RESULTS.md قسم 6. اكتُشفت وأُصلحت 4 مشاكل حقيقية أثناء التنفيذ على
الجهاز الفعلي (القسم 1).

## 0) المتبقي: اختياري بالكامل، غير ضروري لاكتمال الهدف

**تم فعلياً:** نقل الملفات، تثبيت onnxruntime+pycuda، قياس FPS عبر CPU
وGPU (TensorRT) معاً للأوضاع الثلاثة، بأرقام حقيقية موثَّقة بـRESULTS.md.

**المتبقي (اختياري):**
1. اختبار end-to-end بكاميرا حقيقية (`run_camera.py`/`stream_server.py`
   بـ`--backend onnx`) - معلَّق بانتظار توفر كاميرا مع اللوحة، لا علاقة له
   بجاهزية الكود ولا يغيّر رقم الأداء المُقاس.
2. (اختياري) تكرار نفس الخطوات على Raspberry Pi 4 إن توفر وقت.

## 1) 4 مشاكل حقيقية اكتُشفت فقط عند التشغيل على Jetson Nano الفعلي

هذه المشاكل **ما كان ممكن اكتشافها على جهاز التطوير** - ظهرت فقط بعد النقل
الفعلي للعتاد المستهدف:

- **`Illegal instruction (core dumped)` عند استيراد numpy**: عطل معروف على
  Jetson Nano (Cortex-A57) بسبب اكتشاف OpenBLAS الخاطئ لنوع المعالج. الحل:
  `export OPENBLAS_CORETYPE=ARMV8` قبل أي تشغيل بايثون. **ليس عطلاً بكودنا**
  بل خاصية بيئة/عتاد يجب تذكّرها بأي جلسة عمل جديدة على نفس اللوحة.
- **`onnxruntime` رفض تحميل ملفات ONNX**: "Unsupported model IR version:
  10, max supported IR version: 8". السبب: مُصدِّر PyTorch→ONNX الجديد
  (dynamo) يكتب IR version 10/opset 18 دائماً بغض النظر عن `opset_version`
  المطلوب. أُصلح في `export_onnx.py` بإجبار المُصدِّر القديم
  (`dynamo=False`) فيكتب opset 13/IR version 7 مباشرة - متوافق تماماً مع
  `onnxruntime 1.10.0` (أقصى إصدار متاح لبايثون 3.6 على JetPack بالجهاز).
  **الملفات بمجلد `exports/` محدَّثة بالفعل بهذا الإصلاح.**
- **`export_tensorrt.py` مكتوب بواجهة TensorRT حديثة (8.5+)** لكن الجهاز
  فيه TensorRT 8.2.1 (واجهة `get_binding_shape`/`num_bindings` القديمة).
  أُصلح باستخدام الواجهة القديمة المتوافقة.
- **تسريب مرجع ذاكرة GPU** بنفس الملف: تخزين `int(device_mem)` فقط بدون
  إبقاء مرجع بايثون للكائن يجعل garbage collector يحرّر الذاكرة قبل
  `execute_v2` - يسبب "unspecified launch failure" يُفسد جلسة CUDA كاملة.
  أُصلح بإبقاء كل تخصيصات الذاكرة (`device_buffers`) حيّة طوال التنفيذ.

---

## 2) أدوات التصدير/الضغط — بُنيت واختُبرت فعلياً، بعضها معطوب فعلياً

| الأداة | النتيجة الحقيقية |
|---|---|
| `export_onnx.py` | ✅ يعمل، مطابق لـPyTorch (فرق أقصى 7e-6). مُصلَح لكتابة opset 13/IR version 7 (راجع القسم 1) |
| `onnx_inference.py` | ✅ **يعمل بشكل صحيح تماماً - mAP@0.5=38.78% مطابق حرفياً لـPyTorch**، ومُختبَر فعلياً على Jetson Nano حقيقي. هذا هو مسار النشر الموصى به |
| `export_tflite.py` | ⚠️ يُنتج ملفات "تعمل" بلا تحطّم لكن **معطوبة فعلياً**: raw ONNX مقابل TFLite float16 على نفس المدخل يعطي فرقاً حتى 12.4 (يجب أن يكون شبه صفر). السبب المرجّح: onnx2tf يُبعثر ترتيب anchors/القنوات أثناء تحويل NCHW→NHWC |
| `tflite_inference.py` | ❌ **مؤكَّد معطوب**: mAP@0.5 ≈ 0.00% (float16 وINT8 كلاهما) على كامل val set. **لا تستخدمه للنشر** حتى يُحلّ سبب onnx2tf الجذري |
| `export_tensorrt.py` | ✅ **مُختبَر فعلياً وناجح للأوضاع الثلاثة** - محركات FP16 حقيقية على Jetson Nano: ECONOMY 176.7، NORMAL 105.7، TURBO 95.8 FPS، بعد إصلاح مشكلتين (توافق واجهة TensorRT + تسريب ذاكرة GPU) |
| `prune.py` | ✅ يعمل، لكن **30% و10% كلاهما يدمّران الدقة بالكامل** (0%/0.02% mAP) |
| `quantize.py` (PyTorch-native INT8) | ✅ يعمل بعد إصلاح عطلين (SE blocks + residual add) لكن **لا يقلّل الحجم فعلياً** (BatchNorm غير مدموج) |
| `benchmark.py` | ✅ **مُختبَر فعلياً على Jetson Nano** - 10.1-21.4 FPS حسب الوضع (راجع RESULTS.md قسم 6) |

**ملاحظات هامة يجب معرفتها:**
- **إذا احتجت TFLite فعلياً لاحقاً** (مثلاً لـAndroid تحديداً حيث ONNX
  Runtime أقل انتشاراً): الخطأ لم يُحلّ جذرياً، يحتاج تصحيح ترتيب anchors
  بمطابقة تفصيلية بين تخطيط onnx2tf الداخلي وترتيب توليدنا للـanchors - عمل
  تصحيح إضافي غير بسيط.
- **ai_edge_torch (المذكورة بالتقرير) خطيرة**: تخفّض إصدار PyTorch تلقائياً
  وتكسر توافقه مع torchvision بالكامل. لا تُثبَّت في `assd_env` مطلقاً - إن
  احتجت TFLite استخدم بيئة `tflite_env/` المعزولة (Python 3.11) فقط لخطوة
  التحويل، وليس للتشغيل الفعلي طالما العطل أعلاه غير محلول.
- **التقليم البنيوي (Pruning) غير قابل للتطبيق بالطريقة الساذجة** على
  MobileNetV3-Small: حتى 10% تدمّر الدقة بالكامل بدون إعادة تدريب.
- **fbgemm (المذكور بالتقرير) خطأ لـRaspberry Pi**: يحتاج `qnnpack` (ARM).

## 3) الفجوة المتبقية عن 71.4% mAP — ما زال مفتوحاً

النتيجة الحالية: NORMAL=38.78%, TURBO=42.81%, ECONOMY=34.81% (v2، مؤكَّدة
عبر backend=pytorch وbackend=onnx معاً - نفس الرقم بالضبط).

- [ ] **Ablation Study** حقيقي: **لم يُنفَّذ بعد** - يحتاج ~12-16 ساعة تدريب
      متتالي، قرار مقصود بعدم إطلاقه بدون إشراف طويل.
- [ ] عدد حقب أكبر من 100، أو batch size أكبر (مقيّد حالياً بالـCPU).
- [ ] Random Crop الحالي مبسّط - نسخة SSD الأصلية تستخدم قيود IoU أدق.
- [ ] Knowledge Distillation من نموذج VGG-16 معلَّم (توصية التقرير، قسم 5.4).

## 4) ما تم إنجازه سابقاً (مرجع تاريخي)

- [x] إصلاح خطأي فهرسة الأصناف المزدوجة وعدم تطابق الإحداثيات بالتقييم (v1).
- [x] Data Augmentation حقيقي + K-Means anchors + round-robin + LR warmup (v2).
- [x] تدريب نموذجين كاملين (100 epoch لكل واحد) وتقييمهما على 3 أوضاع.
- [x] تنظيف تاريخ git من دمج خاطئ لـ2GB بيانات، ورفع كل شيء لـ GitHub.
- [x] اختبار `stream_server.py` بالنموذج المدرَّب الحقيقي عبر 3 مسارات
      (pytorch، tflite [معطوب، تم اكتشافه]، onnx [صحيح ومؤكَّد]).
- [x] نقل مشروع النشر لـJetson Nano فعلي، تثبيت onnxruntime، قياس FPS حقيقي
      (10.1-21.4 FPS)، وإصلاح عطلي التوافق (OPENBLAS_CORETYPE + IR version).
- [x] بناء واختبار محركات TensorRT حقيقية (FP16) للأوضاع الثلاثة على نفس
      الجهاز - **95.8-176.7 FPS** (تسريع 8.3×-9.5× عن CPU)، بعد إصلاح
      مشكلتي توافق واجهة TensorRT وتسريب مرجع ذاكرة GPU.

## 5) ملاحظة منهجية (تأكدت مرات عديدة هذه الجلسة)

كل ميزة جديدة يجب اختبارها فعلياً بقياس **دقة حقيقية (mAP)** فور بنائها، لا
الاكتفاء بـ"هل يعمل بدون تحطّم؟". أخطر اكتشاف هذه الجلسة: ملفات TFLite
عملت تماماً بلا أي خطأ تنفيذي (تحميل، استدلال، أشكال مخرجات صحيحة) بينما
كانت تنتج نتائج عشوائية تماماً (mAP≈0%) - لم يظهر هذا إلا بقياس mAP الفعلي
على بيانات val ثم فحص IoU يدوي مباشر. "يعمل بدون عطل" ≠ "صحيح".
