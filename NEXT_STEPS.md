# قائمة المهام المتبقية — A-SSD

آخر تحديث: بعد تدريب v2 المحسّن (100 epoch، augmentation حقيقي + K-Means
anchors + round-robin + LR warmup).

## 0) خطة الجلسة القادمة — بالترتيب

**سؤال مفتوح وحيد يحتاج إجابتك (لا أقدر أحسمه لوحدي):** هل يوجد وصول فعلي
لجهاز Raspberry Pi 4 و/أو Jetson Nano حقيقي (عندك أو تقدر تؤمّنه)؟ هذا يحدد
هل نقدر نحقق الهدف الحقيقي للمشروع (تشغيل + قياس فعلي على أجهزة الحافة) أو
نبقى نحضّر فقط الأدوات اللازمة لليوم اللي يتوفر فيه الجهاز.

**سأبدأ الليلة تلقائياً (عمل روتيني آمن، لا يحتاج عتاد ولا قراراً إضافياً):**
1. `export_onnx.py` — تصدير checkpoints_v2/last.pth إلى ONNX + التحقق أنه
   يعمل فعلياً عبر onnxruntime محلياً.
2. `export_tflite.py` — تحويل إلى TFLite (ضروري لـ Raspberry Pi/Android) +
   التحقق أنه يعمل فعلياً عبر tflite runtime محلياً.
3. `prune.py` — تقليم بنيوي (القسم 4.3) + قياس حجم النموذج قبل/بعد + إعادة
   تقييم mAP سريع للتأكد أن التقليم لم يكسر الدقة بشكل كبير.
4. `quantize.py` — تكميم INT8 بعد التدريب + نفس التحقق (حجم + mAP).
5. سكربت تصدير TensorRT سيُكتب أيضاً لكن **لن يُختبر فعلياً هنا** - يحتاج
   NVIDIA GPU/Jetson مع TensorRT مثبَّت، غير متوفر على هذا الجهاز.
6. Ablation Study مصغّر (حقب أقل، ~30 بدل 100) لعزل أثر كل وحدة (ARM/DFMS/
   K-Means anchors) على حدة، مطابقةً لجدول 5-1 بالتقرير.

**ينتظر قرارك أو توفّر العتاد:**
- القياس الفعلي على Raspberry Pi 4 / Jetson Nano (الهدف الحقيقي) - لا يمكن
  تنفيذه من هذا الجهاز إطلاقاً، يحتاج وصولاً فعلياً للعتاد المستهدف.

---

**الوضع الحالي:** خط الأنابيب كامل (anchors + loss + تدريب + تقييم + بث حي)
مُثبَت وصحيح فعلياً. نموذجان مدرَّبان فعلياً بنتائج حقيقية على VOC2012 val:

| الوضع | v1 (`checkpoints/`) | v2 (`checkpoints_v2/`) | التحسّن |
|---|---|---|---|
| NORMAL | 31.85% | **38.78%** | +6.9 نقطة |
| TURBO | 31.74% | **42.81%** | +11.1 نقطة |
| ECONOMY | 28.13% | **34.81%** | +6.7 نقطة |

(المرجع بالتقرير: 71.4% على وضع NORMAL)

---

## 1) ما تم إصلاحه في v2 (لم يعد أولوية)

- [x] تفعيل Data Augmentation فعلياً (Random Crop مع تصحيح الصناديق + Horizontal
      Flip + Color Jitter) داخل `train_one_epoch()`.
- [x] ربط K-Means anchors بالتدريب (`utils.compute_layer_anchor_wh()`)، محسوبة
      من التوزيع الحقيقي لصناديق VOC2012 ومحفوظة داخل كل checkpoint.
- [x] round-robin بدل الاختيار العشوائي للأوضاع الثلاثة أثناء التدريب — حسّن
      TURBO تحديداً بشكل واضح (كان يساوي NORMAL تقريباً، أصبح الأفضل فعلياً).
- [x] إضافة LR warmup (5 حقب) قبل جدولة الخفض الأصلية.
- [x] دعم `--resume` في `train.py`.
- [x] تقييم mAP لكل الأوضاع الثلاثة (NORMAL/TURBO/ECONOMY) على v1 وv2.
- [x] اختبار `stream_server.py` بالنموذج المدرَّب الحقيقي (نجح، بلا أعطال).
- [x] رفع الكود والنتائج لـ GitHub (بعد تنظيف تاريخ git من دمج خاطئ لمجموعة
      بيانات VOC2012 بالكامل، 2GB+ - كان سيمنع الرفع نهائياً).

## 2) الفجوة المتبقية عن 71.4% — ما زال مفتوحاً

38.78% (NORMAL) لا يزال بعيداً عن 71.4%. أسباب محتملة لم تُختبر بعد:
- [ ] **Ablation Study** حقيقي: تعطيل كل وحدة تكيفية (ARM/DFMS/DIC) على حدة
      وقياس أثرها الفعلي، مطابقةً لجدول 5-1 بالتقرير.
- [ ] عدد حقب أكبر من 100، أو batch size أكبر (مقيّد حالياً بالـCPU).
- [ ] Random Crop الحالي مبسّط (معايير min_scale/aspect عشوائية بسيطة) - نسخة
      SSD الأصلية تستخدم قيود IoU أدق مع الصناديق الحقيقية أثناء الاقتصاص.
- [ ] لا يوجد Knowledge Distillation من نموذج VGG-16 معلَّم (يقترحه التقرير
      كتوصية مستقبلية، قسم 5.4).
- [ ] مراجعة ما إذا كانت k=4/6 لكل طبقة (تصميم أصلي) هي الأمثل، أو تحتاج
      تعديلاً بعد إدخال K-Means.

## 3) لم يُختبر أو يُقاس بعد

- [ ] لا يوجد قياس أداء حقيقي على Raspberry Pi 4 أو Jetson Nano فعلياً — كل
      الأرقام حالياً من هذا الجهاز (CPU مطوّر) وغير قابلة للمقارنة المباشرة
      بجداول التقرير (4-1، 4-2). **هذا هو الهدف الحقيقي للمشروع - أولوية عليا.**

## 4) ميزات مذكورة بالتقرير ولم تُبنَ كملفات مشروع بعد

- [ ] **Pruning** (التقليم البنيوي) — قسم 4.3، موجود كمثال كود بالـPDF فقط.
- [ ] **Quantization (INT8)** — نفس الحالة، غير مطبَّق كسكربت فعلي.
- [ ] **التصدير**: ONNX → TensorRT (لـ Jetson Nano) وTFLite (لـ Raspberry
      Pi/Android) — قسم 4.4، غير مطبَّق كسكربتات فعلية. هذه الخطوة ضرورية قبل
      أي اختبار فعلي على الأجهزة المستهدفة.

## 5) ملاحظة منهجية مهمة للمستقبل

كل ميزة جديدة (تصدير، تكميم، ablation...) يجب اختبارها فعلياً فور بنائها على
بيانات/نموذج حقيقي، وليس الاكتفاء بقراءة الكود. هذا أثبت نفسه مرتين الآن:
اكتشاف خطأي فهرسة الأصناف/تطابق الإحداثيات في v1، وتأكيد أن التحسينات
الأربعة في v2 (augmentation + K-Means + round-robin + warmup) أعطت تحسّناً
حقيقياً مقاساً (وليس افتراضاً) بفضل تشغيل تدريب وتقييم كاملين فعلياً.
