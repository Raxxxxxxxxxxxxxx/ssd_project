# قائمة المهام المتبقية — A-SSD

آخر تحديث: بعد أول تدريب كامل (100 epoch) وأول تقييم حقيقي.

**الوضع الحالي:** خط الأنابيب (anchors + loss + تدريب + تقييم) مُثبَت وصحيح فعلياً
بعد إصلاح خطأين حقيقيين. `checkpoints/last.pth` نموذج حقيقي يعمل بدقة
**mAP@0.5 = 31.85%** على وضع NORMAL (المرجع بالتقرير: 71.4%).

---

## 1) أسباب مباشرة للفجوة بين 31.85% و71.4% — أولوية عالية

- [ ] **تفعيل Data Augmentation فعلياً**: `random_horizontal_flip()` في `train.py`
      معرّفة لكن غير مستدعاة داخل `train_one_epoch()`. يجب استدعاؤها على كل
      عينة قبل `preprocess()`. كذلك التقرير (قسم 2.7) يذكر أيضاً Random Crop
      وColor Jitter وهما غير موجودين إطلاقاً — يحتاجان تنفيذاً من الصفر
      (Random Crop يتطلب تعديل صناديق الحقيقة معه أيضاً).
- [ ] **ربط K-Means anchors بالتدريب**: الدالة `utils.kmeans_anchor_boxes()`
      جاهزة لكن غير مستخدمة. يجب حساب أبعاد الـanchors الفعلية من صناديق
      VOC2012 الحقيقية (K=3 لـTURBO، K=6 لـNORMAL... راجع القسم 3.7) واستبدال
      `LAYER_CONFIG` الثابت في `utils.py` بقيم محسوبة من البيانات.
- [ ] **مراجعة استراتيجية تدريب الأوضاع الثلاثة**: حالياً `random.choice(modes)`
      لكل batch يعني كل وضع يتدرب ~1/3 من الوقت فقط. تقييم البديل: تمرير
      نفس الـbatch على الأوضاع الثلاثة وجمع الخسارة (أبطأ لكن كل وضع يتعلم
      بالكامل كل مرة)، أو زيادة عدد الحقب تعويضاً.
- [ ] **إضافة LR warmup** (تحسين غير مذكور بالتقرير لكنه ممارسة قياسية تفيد
      الاستقرار في أول عدة حقب).
- [ ] **دعم `--resume` في train.py** لإكمال التدريب من `checkpoints/last.pth`
      الحالي بعد تطبيق الإصلاحات أعلاه، بدل إعادة كل الـ100 حقبة من الصفر.

## 2) لم يُختبر أو يُقاس بعد

- [ ] تقييم mAP لوضعي **TURBO** و**ECONOMY** (قِسنا NORMAL فقط حتى الآن) عبر
      `evaluate.py --mode TURBO` و`--mode ECONOMY`.
- [ ] **Ablation Study** حقيقي: تعطيل كل وحدة تكيفية (ARM/DFMS/DIC) على حدة
      وقياس أثرها الفعلي، مطابقةً لجدول 5-1 بالتقرير.
- [ ] اختبار `stream_server.py` و`run_camera.py` مع الـcheckpoint **المدرَّب
      الحقيقي الجديد** (آخر اختبار مباشر كان بأوزان عشوائية قبل التدريب).
- [ ] لا يوجد قياس أداء حقيقي على Raspberry Pi 4 أو Jetson Nano فعلياً — كل
      الأرقام حالياً من هذا الجهاز (CPU مطوّر) وغير قابلة للمقارنة المباشرة
      بجداول التقرير (4-1، 4-2).

## 3) ميزات مذكورة بالتقرير ولم تُبنَ كملفات مشروع بعد

- [ ] **Pruning** (التقليم البنيوي) — قسم 4.3، موجود كمثال كود بالـPDF فقط.
- [ ] **Quantization (INT8)** — نفس الحالة، غير مطبَّق كسكربت فعلي.
- [ ] **التصدير**: ONNX → TensorRT (لـ Jetson Nano) وTFLite (لـ Raspberry
      Pi/Android) — قسم 4.4، غير مطبَّق كسكربتات فعلية.

## 4) ملاحظة منهجية مهمة للمستقبل

كل ميزة جديدة (تصدير، تكميم، K-Means...) يجب اختبارها فعلياً فور بنائها على
بيانات/نموذج حقيقي، وليس الاكتفاء بقراءة الكود — هذا بالضبط كيف اكتُشف
الخطآن الحقيقيان (فهرسة الأصناف المزدوجة، وعدم تطابق نظام الإحداثيات) في
هذه الجولة: لم يظهرا إلا بعد تشغيل تدريب وتقييم فعليين على بيانات حقيقية.
